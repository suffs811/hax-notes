### sudo pip install sshuttle
 
### sshuttle -r root@<compromised_ip> <new_network/range> --ssh-cmd "ssh -i pivot"

while the tool is running, you can run commands as norming but it will be through the proxy